{
  "name": "emfed",
  "version": "1.3.0",
  "description": "embed a Mastodon feed in a webpage",
  "main": "dist/emfed.js",
  "type": "module",
  "scripts": {
    "build": "tsc && esbuild --bundle --outdir=dist lib/emfed.js",
    "prepack": "tsc && esbuild --bundle --outdir=dist lib/emfed.js"
  },
  "dependencies": {
    "dompurify": "^2.4.1"
  },
  "devDependencies": {
    "esbuild": "^0.15.16",
    "typescript": "^5.2.2",
    "@types/dompurify": "^3.0.2"
  }
}
